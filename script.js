// Replace these demonstration descriptions with real projects when available.
const projects = [
  {
    "id": "type-study",
    "title": "光纖-環保植物皮革應用於家飾燈具之研究",
    "category": "商品設計/植物皮革/ 農業廢棄物再利用/永續材料/燈具設計",
    "cover": "guang-xian-cover.jpg",
    "preview": "guang-xian-cover.jpg",
    "link": "#project/type-study",
    "description": "環保植物皮革應用於家飾燈具之研究。",
    "demo": false,
    "sections": [
      {
        "title": "摘要",
        "paragraphs": [
          "在永續設計逐漸成為全球趨勢的背景下，材料選擇不再僅是功能與美觀的考量，更涉及環境影響、資源循環與社會責任。傳統動物皮革的加工過程需耗費大量水資源與化學藥劑，並與動物屠宰產業鏈相互依存：而人造皮革多以聚氨酯材料製成，其製程需使用大量有機溶劑，對環境與人體健康皆可能造成負擔。相較之下，植物皮革以鳳梨葉、香蕉莖、蘋果渣等農業副產物製成，不僅不含動物成分，且具備可降解、低污染與友善動物等優勢，成為更符合永續理念的新型材料。",
          "本研究以「水果原生材質×零農廢再製」為核心理念，運用鳳梨農業廢棄物製成的純素植物皮革，結合植物纖維紙，設計一系列具文化寓意或永續價值的水果造型燈具。設計概念源自日常生活中的「創皮、剝果」動作，將果皮片狀化與幾何化，轉化為層層堆疊的結構語彙，使光線從皮革縫隙中柔和透出，呈現溫潤且富有層次的光影效果。透過材料、結構與光影的整合，讓照明產品同時具有功能性、美感與情感連結。"
        ]
      },
      {
        "title": "專案角色｜企劃・商品設計・AI 視覺呈現",
        "paragraphs": [
          "本專案由水果的形態與使用行為出發，探索水果特徵如何轉化為燈具設計。我主要負責企劃發想與商品設計，並以**香蕉燈作為主要實體製作成果**，將香蕉的分瓣結構與「剝皮」動作轉化為燈具的造型與互動方式。",
          "透過拉鍊作為開合機構，使用者可以像剝香蕉一樣逐步打開外層，使光線隨著開啟程度釋放，讓水果意象不只停留在外觀，而是進一步成為產品的操作語言。",
          "在成果呈現上，搭配 **AI 生成工具進行商品視覺與情境模擬**，補充實體模型在不同光線、背景與使用狀態下的視覺表現。"
        ]
      }
    ],
    "images": [
      {
        "src": "guang-xian-01.jpg",
        "alt": "香蕉立地燈設計與開合展示"
      },
      {
        "src": "guang-xian-02.jpg",
        "alt": "香蕉燈外層局部開啟視覺"
      },
      {
        "src": "guang-xian-03.jpg",
        "alt": "香蕉燈分瓣結構展開視覺"
      },
      {
        "src": "guang-xian-04.jpg",
        "alt": "香蕉燈外層展開視覺"
      },
      {
        "src": "guang-xian-05.jpg",
        "alt": "香蕉燈拉鍊開合與光線情境"
      },
      {
        "src": "guang-xian-06.jpg",
        "alt": "蘋果吊燈設計與開合展示"
      },
      {
        "src": "guang-xian-07.jpg",
        "alt": "鳳梨桌燈設計與光影展示"
      }
    ],
    "certificate": "fada-certificate.jpg",
    "award": "2026 FADA UK 產品概念設計類 銀獎",
    "extraAwards": [
      "創新設計實務研討會-環保植物皮革應用於家飾燈具之研究論文發表"
    ],
    "extraCertificates": [
      {
        "src": "guang-xian-conference.png",
        "label": "論文發表證明"
      }
    ],
    "action": {
      "label": "點擊查看企劃書",
      "url": "guang-xian-report.pdf"
    }
  },
  {
    "id": "mori",
    "title": "Meow Meow Smile",
    "category": "寓教於樂/口腔保健/可愛畫風/親子互動",
    "cover": "meow-01.jpg",
    "preview": "meow-01.jpg",
    "link": "#project/mori",
    "description": "《Meow Meow Smile》是一款以大富翁形式進行的教育型桌遊，專為10歲以上孩童設計。",
    "demo": false,
    "blocks": [
      {
        "title": "",
        "paragraphs": [
          "《Meow Meow Smile》是一款以大富翁形式進行的教育型桌遊，專為10歲以上孩童設計。"
        ]
      },
      {
        "title": "",
        "paragraphs": [
          "本作品融合口腔健康知識與聯合國永續發展目標（SDGs 3、12），透過遊戲機制如蛀牙因子卡、問題卡與正確刷牙技巧挑戰，讓玩家在趣味互動中學習護牙觀念。整體設計以可愛風格呈現，搭配注音輔助與環保材質，提升遊戲的可親性與教育意義。本人擔任主美術設計，負責角色形象、卡牌插圖與地圖構成，並與團隊共同完成遊戲企劃與測試優化。"
        ]
      },
      {
        "images": [
          {
            "src": "meow-02.jpg",
            "alt": "我的核心參與項目"
          },
          {
            "src": "meow-03.jpg",
            "alt": "大富翁地圖"
          }
        ]
      },
      {
        "images": [
          {
            "src": "meow-04.jpg",
            "alt": "事件卡"
          }
        ]
      },
      {
        "title": "卡牌設計-事件卡",
        "paragraphs": [
          "事件卡是推動遊戲節奏與教育內容的關鍵設計，玩家踩到事件格時需立即抽卡執行，內容包含趣味挑戰與牙齒健康情境，如模擬就診的「醫院掛號卡」與教學導向的「正確刷牙技巧卡」，加強玩家對口腔保健的認識。視覺設計上採用色彩分類、圖像主導、注音輔助與統一圖示，降低閱讀門檻並提升辨識度，使兒童能輕鬆理解並參與，實現寓教於樂的遊戲體驗。"
        ],
        "pointsUp": true
      },
      {
        "images": [
          {
            "src": "meow-05.jpg",
            "alt": "貓咪魔法卡"
          },
          {
            "src": "meow-06.jpg",
            "alt": "貓咪魔法卡展示"
          }
        ]
      },
      {
        "title": "卡牌設計-貓咪魔法卡",
        "paragraphs": [
          "貓咪魔法卡是提升遊戲策略性的重要輔助卡牌，玩家可透過使用它解除蛀牙因子、轉移負面狀態、換位、啟動護盾等特殊效果，扭轉戰局、保護自己或影響對手。卡牌設計魔法卡以活潑的黃色為主色，象徵魔法與轉機，搭配閃耀的星星與魔杖圖樣，營造出魔幻童趣的氛圍。，融入可愛插圖與注音輔助，讓年幼玩家也能輕鬆上手，在趣味操作中學習健康管理概念，提升遊戲的教育性與互動性。"
        ],
        "pointsUp": true
      },
      {
        "images": [
          {
            "src": "meow-07.jpg",
            "alt": "蛀牙因子卡"
          },
          {
            "src": "meow-08.jpg",
            "alt": "蛀牙因子卡展示"
          }
        ]
      },
      {
        "title": "卡牌設計-蛀牙因子卡",
        "paragraphs": [
          "蛀牙因子卡模擬現實生活中會危害口腔健康的行為與狀態，如糖尿病、檳榔、抽菸、甜食、含糖飲料與懶惰刷牙等，讓玩家在遊戲中認識這些看似日常但實則危險的蛀牙因子。每位玩家起始隨機持有兩張卡牌，象徵每個人一開始可能就潛藏牙齒健康風險，必須在遊戲過程中透過解除行動或魔法卡來應對與排除。"
        ],
        "pointsUp": true
      },
      {
        "images": [
          {
            "src": "meow-09.jpg",
            "alt": "問題卡"
          }
        ]
      },
      {
        "title": "卡牌設計-問題卡",
        "paragraphs": [
          "問題卡是本桌遊中結合知識與遊戲性的核心元素，透過口腔健康相關的選擇題，引導玩家在遊戲中學習正確護牙觀念。除了以「淺顯易懂＋情境式提問」的方式呈現題目，讓孩童能透過遊戲連結生活經驗，更加入了答對可得好牙、答錯需交出好牙的獎懲機制，增添競爭與學習動機。視覺上則搭配注音輔助與情境插圖，即使是識字能力尚未成熟的兒童也能透過圖片理解題意，降低閱讀門檻，強化遊戲的教育性與可親性。"
        ],
        "pointsUp": true
      },
      {
        "title": "角色設計-勇士貓咪",
        "pointsUp": false,
        "characters": [
          {
            "src": "meow-10.png",
            "alt": "勇士貓咪：弓箭手"
          },
          {
            "src": "meow-11.png",
            "alt": "勇士貓咪：刺客"
          },
          {
            "src": "meow-12.png",
            "alt": "勇士貓咪：魔法師"
          },
          {
            "src": "meow-13.png",
            "alt": "勇士貓咪：騎士"
          }
        ]
      },
      {
        "images": [
          {
            "src": "meow-14.jpg",
            "alt": "遊戲盒子"
          }
        ]
      },
      {
        "title": "遊戲盒子",
        "paragraphs": [
          "《Meow Meow Smile》的遊戲盒封面以可愛童趣為主，展現親和力與教育性，融合牙齒保健元素與遊戲世界觀。擬人化的勇士貓角色牙齒們及甜點場景互動，呼應遊戲主題與口腔健康教育。明亮的粉藍與粉紅配色，搭配圓潤可愛的字體，吸引兒童家庭玩家，傳達輕鬆歡樂的氛圍及「透過遊戲學習護牙」的理念。"
        ],
        "pointsUp": true
      },
      {
        "images": [
          {
            "src": "meow-15.jpg",
            "alt": "明信片正面"
          },
          {
            "src": "meow-16.jpg",
            "alt": "明信片背面"
          }
        ]
      },
      {
        "title": "周邊商品-明信片",
        "paragraphs": [
          ""
        ],
        "pointsUp": true
      },
      {
        "images": [
          {
            "src": "meow-17.jpg",
            "alt": "桌遊完整展示"
          }
        ]
      }
    ],
    "certificate": "egda-certificate.png",
    "award": "2024 EGDA全球華人教育遊戲設計大賞 優選獎",
    "action": {
      "label": "點擊查看遊戲介紹",
      "url": "https://www.youtube.com/watch?v=pKjXmwu7z50"
    }
  },
  {
    "id": "everyday",
    "title": "有求必應，好神旺旺",
    "category": "時報金犢獎-旺旺集團行銷企劃案",
    "cover": "wang-cover-logo.svg",
    "preview": "wang-cover-logo.svg",
    "link": "#project/everyday",
    "description": "",
    "demo": false,
    "award": "金犢獎－旺旺集團行銷企劃案創作獎",
    "certificate": "wang-certificate.jpg",
    "action": {
      "label": "點擊查看企劃書",
      "url": "wang-proposal.pdf"
    },
    "images": [
      {
        "src": "wang-contribution.png",
        "alt": "我的核心參與項目：企劃撰寫與結構統整、品牌策略與風格設計、美術統整與版面優化、視覺角色設計"
      }
    ],
    "productCards": [
      {
        "src": "wang-product-01.png",
        "title": "旺旺仙貝 財力翻倍",
        "text": "以財犬、財神為靈感，重新詮釋旺旺仙貝包裝，象徵著「財富旺旺、財運旺旺」。並將旺旺仙貝設計成金元寶造型，與財神更具連結性。"
      },
      {
        "src": "wang-product-02.png",
        "title": "旺旺果粒多 × 拜財神限定包裝",
        "text": "將旺旺果粒多設計為紅包袋造型，傳遞送禮吉祥與財富寓意，讓消費者在享用果粒多的同時，彷彿是在將財富注入口中，增添互動趣味與祝福滿滿的心情。"
      },
      {
        "src": "wang-product-03.png",
        "title": "財運隨行・招財悠遊卡",
        "text": "以「拜財神」為核心主題，設計結合角色插畫與吉祥語的悠遊卡，搭配節慶紅金色系與招財元素（如銅錢、財神爺、祝福語），讓悠遊卡不僅具實用性，更成為年輕族群喜愛的收藏性商品。"
      },
      {
        "src": "wang-product-04.png",
        "title": "拜財神綜合包",
        "text": "整體以「紅色聚寶袋」造型為基礎，象徵裝滿財富與好運。以放射狀金光與產品飛躍的畫面營造「打開福袋、財氣四散」的喜氣感，視覺焦點集中又具動態感。"
      },
      {
        "src": "wang-product-05.png",
        "title": "旺旺御守系列",
        "text": "為延伸「拜財神」節慶活動與品牌互動經驗，特別設計三款御守商品，結合年輕族群常見的祈願主題（招財、戀愛、學業），搭配旺旺品牌角色，讓御守不只是祈福道具，更成為趣味十足、充滿收藏價值的限定周邊。"
      },
      {
        "src": "wang-product-06-v2.png",
        "title": "摺紙財神爺 × 小紅書社群互動",
        "text": "透過小紅書推出摺紙財神教學短片，吸引參與者在節慶中與品牌建立互動記憶。"
      }
    ]
  },
  {
    "id": "haike",
    "title": "來chill海科 玩一夏！",
    "category": "海科盃-智慧科普與永續品牌行銷競賽",
    "cover": "haike-cover.jpg",
    "preview": "haike-cover.jpg",
    "link": "#project/haike",
    "description": "",
    "demo": false,
    "action": {
      "label": "點擊查看企劃書",
      "url": "haike-proposal.pdf"
    },
    "award": "海科盃－智慧科普與永續品牌行銷競賽 銀牌",
    "certificate": "haike-certificate.png",
    "certificateLink": "haike-certificate.pdf",
    "blocks": [
      {
        "title": "專案概覽與課題",
        "paragraphs": [
          "本競賽以「智慧科普、永續海洋」為精神，透過海洋觀光遊憩提案，串聯海科館及周邊資源，提升海洋教育、地方觀光與品牌影響力。",
          "以「基隆 × 海洋 × 數位 × 永續」為核心，串聯海科館與周邊景點，打造不同客群的主題旅遊體驗。",
          "如何讓海科館不只是單一參觀景點，而成為探索基隆海洋觀光的起點？"
        ]
      },
      {
        "images": [
          {
            "src": "haike-story-02.jpg",
            "alt": "海科專案展示 2"
          }
        ],
        "layout": ""
      },
      {
        "title": "專案角色｜田野調查・企劃發想・企劃書製作・簡報設計",
        "paragraphs": [
          "從海科館與周邊場域調查出發，參與旅遊企劃概念與路線發想，並負責企劃內容統整及競賽提案簡報的視覺呈現。"
        ]
      },
      {
        "title": "田野調查",
        "paragraphs": [
          "團隊實際前往海科館及八斗子周邊進行田野調查，觀察場域、交通動線與周邊觀光資源。我們發現海科館附近聚集大量自然景觀、漁村文化及休閒景點，但各景點大多以單點方式被體驗，因此開始思考如何透過不同旅遊需求重新串聯。"
        ]
      },
      {
        "images": [
          {
            "src": "haike-story-03.jpg",
            "alt": "海科專案展示 3"
          },
          {
            "src": "haike-story-04.jpg",
            "alt": "海科專案展示 4"
          },
          {
            "src": "haike-story-05.jpg",
            "alt": "海科專案展示 5"
          },
          {
            "src": "haike-story-06.jpg",
            "alt": "海科專案展示 6"
          },
          {
            "src": "haike-story-07.jpg",
            "alt": "海科專案展示 7"
          },
          {
            "src": "haike-story-08.jpg",
            "alt": "海科專案展示 8"
          }
        ],
        "layout": "field-grid"
      },
      {
        "title": "洞察與企劃方向",
        "paragraphs": [
          "景點並不少，缺少的是將景點轉化成完整旅遊體驗的串聯方式。",
          "以海科館為旅程起點，融合地方文化、永續旅遊、青年回流創業與數位行銷，打造可持續發展的海洋觀光聚落。"
        ]
      },
      {
        "title": "五條主題旅遊路線",
        "routes": [
          {
            "title": "01 健行強森",
            "place": "望幽谷／潮境公園",
            "tags": "自然 × 健行 × 生態",
            "color": "#b8dcd1"
          },
          {
            "title": "02 潮玩漁港",
            "place": "八斗子漁港／海景",
            "tags": "親子 × 情侶 × 深度旅遊",
            "color": "#acd4ee"
          },
          {
            "title": "03 基隆出國行",
            "place": "正濱漁港彩色屋",
            "tags": "拍照 × 文化 × 異國感",
            "color": "#f3cfb4"
          },
          {
            "title": "04 我們與海洋的距離",
            "place": "海洋／漁村文化",
            "tags": "教育 × 文化 × 海洋體驗",
            "color": "#c9c6eb"
          },
          {
            "title": "05 這趟我怕練太壯",
            "place": "SUP／海岸",
            "tags": "運動 × 冒險 × 戶外",
            "color": "#efd993"
          }
        ]
      },
      {
        "title": "",
        "paragraphs": [
          "同一個地方資源，可以因為客群不同，被重新組合成不同體驗。"
        ]
      },
      {
        "title": "延伸體驗設計",
        "paragraphs": [
          "將旅遊從「看景點」延伸為「參與體驗」，加入 APP 任務、生物圖鑑、夜釣小卷、一日漁夫、海廢手作等互動內容。"
        ]
      },
      {
        "images": [
          {
            "src": "haike-story-09.jpg",
            "alt": "海科專案展示 9"
          },
          {
            "src": "haike-story-10.png",
            "alt": "海科專案展示 10"
          },
          {
            "src": "haike-story-11.png",
            "alt": "海科專案展示 11"
          },
          {
            "src": "haike-story-12.png",
            "alt": "海科專案展示 12"
          }
        ],
        "layout": "mockup-grid"
      },
      {
        "title": "數位與行銷延伸",
        "paragraphs": [
          "透過 KOL 路線體驗、海洋心理測驗與整合式旅遊平台，將路線推薦、活動報名、任務互動與社群分享串聯起來。"
        ]
      },
      {
        "images": [
          {
            "src": "haike-story-13.png",
            "alt": "海科專案展示 13"
          },
          {
            "src": "haike-story-14.png",
            "alt": "海科專案展示 14"
          },
          {
            "src": "haike-story-15.png",
            "alt": "海科專案展示 15"
          }
        ],
        "layout": "marketing-grid"
      },
      {
        "title": "成果與學習",
        "paragraphs": [
          "從實際場域調查出發，將分散的景點與地方資源轉化為完整觀光企劃，也讓我更熟悉如何從觀察中整理問題、發展概念，並透過資訊設計將複雜的企劃內容轉化成清楚易懂的提案。"
        ]
      }
    ]
  }
];
const windows = [...document.querySelectorAll('.window')];
const opened = new Set(['welcome']);
let layer = 20;
let active = 'welcome';
const lastFocus = new Map();
function raise(win) { win.style.zIndex = ++layer; active = win.id; renderTasks(); }
function openWindow(id, focus = true) {
  const win = document.getElementById(id);
  if (!win) return;
  lastFocus.set(id, document.activeElement);
  win.hidden = false;
  if (id === 'work' || id === 'detail') {
    win.classList.add('maximized');
    const button = win.querySelector('[data-action="maximize"]');
    button?.setAttribute('aria-pressed', 'true');
    if (button) button.title = '還原視窗大小';
  }
  opened.add(id);
  raise(win);
  if (focus) win.querySelector('button')?.focus({ preventScroll: true });
  if (matchMedia('(max-width: 700px)').matches && !win.classList.contains('secondary')) win.scrollIntoView({ behavior: 'smooth', block: 'start' });
  document.getElementById('announcement').textContent = `已開啟 ${win.querySelector('h1,h2').textContent}`;
}
function hideWindow(win, close) {
  win.hidden = true;
  if (close) opened.delete(win.id);
  if (active === win.id) active = windows.filter(w => !w.hidden).sort((a,b) => (+b.style.zIndex || 0) - (+a.style.zIndex || 0))[0]?.id || '';
  renderTasks();
  const target = lastFocus.get(win.id);
  if (target && !target.closest('[hidden]')) target.focus({ preventScroll: true });
  else document.querySelector('.start').focus({ preventScroll: true });
}
function renderTasks() {
  const root = document.getElementById('tasks');
  root.replaceChildren();
  const names = { welcome:'歡迎', work:'精選作品', about:'關於沛璇', contact:'聯絡方式', detail:'作品詳情' };
  opened.forEach(id => {
    const win = document.getElementById(id);
    const button = document.createElement('button');
    button.textContent = names[id];
    button.className = win.hidden ? 'minimized' : active === id ? 'active' : '';
    button.setAttribute('aria-label', `開啟${names[id]}視窗`);
    button.setAttribute('aria-pressed', String(active === id && !win.hidden));
    button.addEventListener('click', () => openWindow(id));
    root.append(button);
  });
}
document.querySelectorAll('[data-open]').forEach(button => button.addEventListener('click', () => openWindow(button.dataset.open)));
document.querySelector('.brand').addEventListener('click', event => { event.preventDefault(); openWindow('welcome'); });
windows.forEach(win => {
  win.addEventListener('pointerdown', () => raise(win));
  win.querySelectorAll('[data-action]').forEach(button => button.addEventListener('click', () => {
    if (button.dataset.action === 'maximize') {
      const maximized = win.classList.toggle('maximized');
      button.setAttribute('aria-pressed', String(maximized));
      button.title = maximized ? '還原視窗大小' : '放大視窗';
    } else hideWindow(win, button.dataset.action === 'close');
  }));
  const bar = win.querySelector('.titlebar');
  bar.addEventListener('pointerdown', event => {
    if (event.target.closest('button') || matchMedia('(max-width:700px)').matches || win.classList.contains('maximized') || event.button !== 0) return;
    const rect = win.getBoundingClientRect();
    const startX = event.clientX, startY = event.clientY;
    win.style.position = 'fixed'; win.style.width = `${rect.width}px`; win.style.margin = '0'; win.style.transform = 'none';
    win.style.left = `${rect.left}px`; win.style.top = `${rect.top}px`;
    bar.setPointerCapture(event.pointerId);
    function move(e) {
      win.style.left = `${Math.max(0, Math.min(innerWidth - rect.width, rect.left + e.clientX - startX))}px`;
      win.style.top = `${Math.max(44, Math.min(innerHeight - 100, rect.top + e.clientY - startY))}px`;
    }
    function end() { bar.removeEventListener('pointermove', move); bar.removeEventListener('pointerup', end); bar.removeEventListener('pointercancel', end); }
    bar.addEventListener('pointermove', move); bar.addEventListener('pointerup', end); bar.addEventListener('pointercancel', end);
  });
});
document.getElementById('theme').addEventListener('click', event => {
  const dark = document.body.classList.toggle('night');
  event.currentTarget.setAttribute('aria-label', dark ? '切換淺色桌布' : '切換深色桌布');
});
let desktopHidden = [];
document.getElementById('show-desktop').addEventListener('click', () => {
  if (windows.some(w => !w.hidden)) {
    desktopHidden = windows.filter(w => !w.hidden).map(w => w.id);
    windows.forEach(w => w.hidden = true);
  } else desktopHidden.forEach(id => { document.getElementById(id).hidden = false; });
  renderTasks();
});
document.addEventListener('keydown', event => {
  if (event.key === 'Escape' && active) hideWindow(document.getElementById(active), false);
});
function clock() {
  const now = new Date();
  document.getElementById('date').textContent = now.toLocaleDateString('en-US', { month:'short', day:'numeric' }).toUpperCase();
  document.getElementById('clock').textContent = now.toLocaleTimeString('en-GB', { hour:'2-digit', minute:'2-digit' });
  document.getElementById('clock').dateTime = now.toISOString();
}
clock(); setInterval(clock, 30000); renderTasks();
// Resize from the lower-right corner; keep the handle inside the usable desktop.
windows.forEach(win => {
  const handle = document.createElement('button');
  handle.className = 'resize-handle';
  handle.setAttribute('aria-label', '調整視窗大小：拖曳或使用方向鍵');
  handle.title = '拖曳調整大小（也可使用方向鍵）';
  win.append(handle);
  function prepare() {
    const rect = win.getBoundingClientRect();
    const left = Math.max(8, Math.min(rect.left, innerWidth - 368));
    const top = Math.max(52, Math.min(rect.top, innerHeight - 300));
    Object.assign(win.style, { position:'fixed', left:`${left}px`, top:`${top}px`,
      margin:'0', transform:'none', width:`${rect.width}px`, height:`${rect.height}px` });
    win.classList.add('user-sized');
    return { width:rect.width, height:rect.height, left, top };
  }
  function size(rect, dx, dy) {
    const maxWidth = innerWidth - rect.left - 8;
    const maxHeight = innerHeight - rect.top - 62;
    win.style.width = `${Math.min(maxWidth, Math.max(360, rect.width + dx))}px`;
    win.style.height = `${Math.min(maxHeight, Math.max(240, rect.height + dy))}px`;
  }
  handle.addEventListener('pointerdown', event => {
    if (event.button !== 0 || win.classList.contains('maximized') || matchMedia('(max-width:700px)').matches) return;
    event.preventDefault();
    const rect = prepare(), x = event.clientX, y = event.clientY;
    size(rect, 0, 0);
    handle.setPointerCapture(event.pointerId);
    win.classList.add('resizing');
    function move(e) { size(rect, e.clientX - x, e.clientY - y); }
    function end() {
      win.classList.remove('resizing');
      handle.removeEventListener('pointermove', move);
      handle.removeEventListener('pointerup', end);
      handle.removeEventListener('pointercancel', end);
      handle.removeEventListener('lostpointercapture', end);
    }
    handle.addEventListener('pointermove', move);
    handle.addEventListener('pointerup', end);
    handle.addEventListener('pointercancel', end);
    handle.addEventListener('lostpointercapture', end);
  });
  handle.addEventListener('keydown', event => {
    if (!['ArrowRight','ArrowLeft','ArrowUp','ArrowDown'].includes(event.key)) return;
    event.preventDefault();
    const step = event.shiftKey ? 40 : 16;
    size(prepare(), event.key === 'ArrowRight' ? step : event.key === 'ArrowLeft' ? -step : 0,
      event.key === 'ArrowDown' ? step : event.key === 'ArrowUp' ? -step : 0);
  });
});

// Reset freely positioned windows when the viewport changes to keep them reachable.
addEventListener('resize', () => windows.forEach(win => {
  win.classList.remove('user-sized');
  if (win.style.position === 'fixed') ['position','width','height','margin','transform','left','top'].forEach(property => win.style.removeProperty(property));
}));

// Lightweight desktop-only pointer trail, with no mouse interception.
(() => {
  const desktop = document.querySelector('.desktop');
  const canvas = document.createElement('canvas');
  canvas.className = 'cursor-trail';
  canvas.setAttribute('aria-hidden', 'true');
  desktop.prepend(canvas);
  const ctx = canvas.getContext('2d');
  if (!ctx) { canvas.remove(); return; }
  const reduced = matchMedia('(prefers-reduced-motion: reduce)');
  const mouse = matchMedia('(hover: hover) and (pointer: fine)');
  let points = [], frame = 0, previous = null;
  const life = 1200;
  const spacing = 32;
  let distanceToNext = spacing;
  const smoothstep = t => t * t * (3 - 2 * t);
  function emitFlower(x, y, time) {
    points.push({x, y, time, scale: .65 + Math.random() * .85,
      rotation: Math.random() * Math.PI * 2,
      turn: (Math.random() - .5) * .5,
      color: Math.random() < .5 ? '#d9a6c3' : '#c4a1d4'});
  }
  function samplePath(x, y, time) {
    if (!previous) {
      emitFlower(x, y, time);
      previous = {x, y}; distanceToNext = spacing; return;
    }
    const dx = x - previous.x, dy = y - previous.y;
    const distance = Math.hypot(dx, dy);
    if (!distance) return;
    let travelled = distanceToNext;
    // Carry spacing across events so slow curves and fast sweeps stay continuous.
    for (; travelled <= distance; travelled += spacing) {
      emitFlower(previous.x + dx * travelled / distance,
        previous.y + dy * travelled / distance, time);
    }
    distanceToNext = travelled - distance;
    previous = {x, y};
  }
  function clear() {
    cancelAnimationFrame(frame); frame = 0; points = []; previous = null;
    ctx.clearRect(0, 0, innerWidth, innerHeight);
  }
  function fit() {
    clear();
    const scale = Math.min(devicePixelRatio || 1, 2);
    canvas.width = Math.round(innerWidth * scale);
    canvas.height = Math.round(innerHeight * scale);
    ctx.setTransform(scale, 0, 0, scale, 0, 0);
  }
  function draw(now) {
    frame = 0;
    ctx.clearRect(0, 0, innerWidth, innerHeight);
    points = points.filter(p => now - p.time < life);
    for (const p of points) {
      const remaining = Math.max(0, 1 - (now - p.time) / life);
      const age = Math.max(0, now - p.time);
      const enter = smoothstep(Math.min(1, age / 110));
      const fade = smoothstep(remaining);
      const size = p.scale * (.65 + .35 * fade) * (.8 + .2 * enter);
      ctx.save();
      ctx.translate(p.x, p.y);
      ctx.rotate(p.rotation + (1 - fade) * p.turn);
      ctx.scale(size, size);
      ctx.globalAlpha = enter * fade * .8;
      ctx.fillStyle = p.color;
      for (let petal = 0; petal < 5; petal++) {
        ctx.save();
        ctx.rotate(petal * Math.PI * 2 / 5);
        ctx.beginPath();
        ctx.ellipse(0, -4.5, 2.8, 4.2, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
      }
      ctx.beginPath();
      ctx.fillStyle = '#fff5f9';
      ctx.arc(0, 0, 2.5, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    if (points.length) frame = requestAnimationFrame(draw);
  }
  document.addEventListener('pointermove', event => {
    if (reduced.matches || !mouse.matches || event.pointerType !== 'mouse' || event.buttons || event.target !== desktop) {
      previous = null; return;
    }
    const now = performance.now();
    const samples = typeof event.getCoalescedEvents === 'function' ? event.getCoalescedEvents() : [];
    for (const sample of samples.length ? samples : [event]) {
      samplePath(sample.clientX, sample.clientY, now);
    }
    if (points.length > 100) points.splice(0, points.length - 100);
    if (!frame) frame = requestAnimationFrame(draw);
  }, {passive: true});
  document.documentElement.addEventListener('pointerleave', () => { previous = null; });
  document.addEventListener('visibilitychange', () => { if (document.hidden) clear(); });
  addEventListener('blur', clear);
  addEventListener('resize', fit);
  addEventListener('scroll', clear, {passive: true});
  reduced.addEventListener('change', clear);
  mouse.addEventListener('change', clear);
  fit();
})();

// Run the opening once per page load. Interaction immediately restores normal positioning.
(() => {
  const welcome = document.getElementById('welcome');
  const finish = () => welcome.classList.remove('welcome-enter');
  welcome.addEventListener('pointerdown', finish, {once: true});
  welcome.addEventListener('focusin', finish, {once: true});
  setTimeout(finish, 2300);
})();

// Data-driven gallery. Replace cover/preview with image URLs and link with the project URL.
(() => {
  const grid = document.getElementById('projects-grid');
  const counter = document.getElementById('project-count');
  counter.textContent = String(projects.length).padStart(2, '0') + ' ITEMS';
  document.getElementById('project-total').textContent = `${projects.length} 個作品`;
  const fine = matchMedia('(hover: hover) and (pointer: fine) and (min-width: 701px)');
  const preview = document.createElement('div');
  preview.className = 'gallery-preview';
  preview.setAttribute('aria-hidden', 'true');
  preview.setAttribute('popover', 'manual');
  const previewImage = new Image(); previewImage.alt = '';
  preview.append(previewImage); document.body.append(preview);
  let current = null, request = 0, hideTimer;
  function hidePreview() {
    current = null; request++; clearTimeout(hideTimer);
    preview.classList.remove('visible'); grid.classList.remove('previewing');
    grid.querySelectorAll('.is-previewed').forEach(card => card.classList.remove('is-previewed'));
    hideTimer = setTimeout(() => { if (!current && preview.matches(':popover-open')) preview.hidePopover(); }, 330);
  }
  function positionPreview(card) {
    const box = card.querySelector('.project-cover').getBoundingClientRect();
    const width = Math.max(1, Math.min(box.width * 1.5, innerWidth - 24, 560));
    const height = Math.max(1, Math.min(box.height * 1.5, innerHeight - 110, 650));
    const left = Math.max(12, Math.min(innerWidth - width - 12, box.left + (box.width - width) / 2));
    const top = Math.max(52, Math.min(innerHeight - height - 58, box.top + (box.height - height) / 2));
    Object.assign(preview.style, {width:`${width}px`,height:`${height}px`,left:`${left}px`,top:`${top}px`});
  }
  function showPreview(card, project) {
    if (!fine.matches) return;
    clearTimeout(hideTimer); current = card;
    const version = ++request;
    preview.classList.remove('visible');
    const image = new Image();
    image.onload = () => {
      if (version !== request || current !== card) return;
      previewImage.src = image.src; positionPreview(card);
      grid.querySelectorAll('.is-previewed').forEach(c => c.classList.remove('is-previewed'));
      card.classList.add('is-previewed'); grid.classList.add('previewing');
      if (typeof preview.showPopover === 'function' && !preview.matches(':popover-open')) preview.showPopover();
      requestAnimationFrame(() => { if (version === request) preview.classList.add('visible'); });
    };
    image.onerror = hidePreview;
    image.src = project.preview || project.cover;
  }
  function renderProjectBlock(body, block) {
    const section = document.createElement('section');
    section.className = 'project-story';
    if (block.title) {
      const heading = document.createElement('h4'); heading.textContent = block.title;
      if (block.pointsUp) {
        const arrow = document.createElement('span'); arrow.className = 'heading-up-arrow'; arrow.textContent = ''; arrow.setAttribute('aria-hidden', 'true'); heading.prepend(arrow);
      }
      section.append(heading);
    }
    for (const paragraph of block.paragraphs || []) {
      if (!paragraph) continue;
      const p = document.createElement('p'); p.textContent = paragraph; section.append(p);
    }
    if (block.images) {
      section.className = 'project-image-sequence' + (block.layout ? ' ' + block.layout : '');
      for (const item of block.images) {
        const link = document.createElement('a'); link.href = item.src; link.target = '_blank'; link.rel = 'noopener';
        link.setAttribute('aria-label', item.alt + '，開啟完整圖片');
        const img = new Image(); img.src = item.src; img.alt = item.alt; img.loading = 'lazy'; img.decoding = 'async';
        link.append(img); section.append(link);
      }
    }
    if (block.routes) {
      const grid = document.createElement('div'); grid.className = 'haike-routes';
      for (const route of block.routes) {
        const card = document.createElement('article'); card.className = 'haike-route';
        const img = document.createElement('div'); img.className = 'haike-route-color'; img.style.backgroundColor = route.color; img.setAttribute('aria-hidden', 'true');
        const h = document.createElement('h5'); h.textContent = route.title;
        const place = document.createElement('p'); place.textContent = route.place;
        const tags = document.createElement('p'); tags.className = 'route-tags'; tags.textContent = route.tags;
        card.append(img,h,place,tags); grid.append(card);
      }
      section.append(grid);
    }
    if (block.characters) {
      const stage = document.createElement('div'); stage.className = 'character-lineup';
      for (const item of block.characters) {
        const img = new Image(); img.src = item.src; img.alt = item.alt; img.loading = 'lazy'; stage.append(img);
      }
      section.append(stage);
    }
    body.append(section);
  }

  function openProject(project) {
    hidePreview();
    const body = document.getElementById('detail-body');
    const image = new Image(); image.src = project.preview || project.cover;
    image.alt = project.title + '完整封面'; image.className = 'project-detail-cover';
    const title = document.createElement('h3'); title.textContent = project.title;
    const category = document.createElement('p'); category.className = 'type'; category.textContent = project.category;
    const text = document.createElement('p'); text.textContent = project.description || '';
    body.classList.toggle('meow-project', project.id === 'mori');
    document.getElementById('detail').classList.toggle('haike-project', project.id === 'haike');
    document.getElementById('detail').classList.toggle('wang-project', project.id === 'everyday');
    const headingRow = document.createElement('div'); headingRow.className = 'project-heading-row';
    const summary = document.createElement('div'); summary.className = 'project-summary';
    const titleLine = document.createElement('div'); titleLine.className = 'project-title-line'; titleLine.append(title);
    if (project.action) {
      const action = document.createElement('a'); action.className = 'project-action'; action.textContent = project.action.label;
      action.href = project.action.url; action.target = '_blank'; action.rel = 'noopener'; titleLine.append(action);
    }
    summary.append(titleLine, category);
    for (const label of [project.award, ...(project.extraAwards || [])].filter(Boolean)) {
      const ribbon = document.createElement('p'); ribbon.className = 'award-ribbon'; ribbon.textContent = label; summary.append(ribbon);
    }
    headingRow.append(summary);
    const certificates = [...(project.certificate ? [{src: project.certificate, href: project.certificateLink, label: project.award + '獎狀'}] : []), ...(project.extraCertificates || [])];
    if (certificates.length) {
      const group = document.createElement('div'); group.className = 'project-certificates';
      for (const item of certificates) {
        const certificate = document.createElement('a'); certificate.className = 'project-certificate';
        certificate.href = item.href || item.src; certificate.target = '_blank'; certificate.rel = 'noopener';
        certificate.setAttribute('aria-label', '放大查看：' + item.label);
        const thumb = new Image(); thumb.src = item.src; thumb.alt = item.label;
        const caption = document.createElement('span'); caption.textContent = '點擊放大查看 ↗';
        certificate.append(thumb, caption); group.append(certificate);
      }
      headingRow.append(group);
    }
    body.replaceChildren(image, headingRow);
    if (project.blocks?.length) {
      project.blocks.forEach(block => renderProjectBlock(body, block));
    } else if (project.sections?.length) {
      for (const section of project.sections) {
        const block = document.createElement('section'); block.className = 'project-story';
        const heading = document.createElement('h4'); heading.textContent = section.title; block.append(heading);
        for (const paragraph of section.paragraphs) {
          const p = document.createElement('p');
          paragraph.split(/(\*\*.*?\*\*)/g).forEach(part => {
            if (part.startsWith('**') && part.endsWith('**')) {
              const strong = document.createElement('strong'); strong.textContent = part.slice(2, -2); p.append(strong);
            } else p.append(document.createTextNode(part));
          });
          block.append(p);
        }
        body.append(block);
      }
    } else body.append(text);
    if (project.images?.length) {
      const gallery = document.createElement('div'); gallery.className = 'project-image-sequence';
      for (const item of project.images) {
        const figure = document.createElement('figure');
        const img = new Image(); img.src = item.src; img.alt = item.alt;
        img.loading = 'lazy'; img.decoding = 'async';
        const link = document.createElement('a'); link.href = item.src; link.target = '_blank'; link.rel = 'noopener';
        link.setAttribute('aria-label', item.alt + '，開啟完整圖片'); link.append(img); figure.append(link); gallery.append(figure);
      }
      body.append(gallery);
    }
    if (project.productCards?.length) {
      const showcase = document.createElement('section'); showcase.className = 'wang-products';
      const heading = document.createElement('h4'); heading.textContent = '「拜財神」';
      const subtitle = document.createElement('p'); subtitle.className = 'wang-products-subtitle'; subtitle.textContent = '旺旺仙貝 財力翻倍：旺旺一口 好運當頭';
      showcase.append(heading, subtitle);
      for (const item of project.productCards) {
        const card = document.createElement('article'); card.className = 'wang-product-card';
        const link = document.createElement('a'); link.href = item.src; link.target = '_blank'; link.rel = 'noopener'; link.className = 'wang-product-art'; link.setAttribute('aria-label', item.title + '，查看原圖');
        const img = new Image(); img.src = item.src; img.alt = item.title; img.loading = 'lazy'; link.append(img);
        const copy = document.createElement('div'); const h = document.createElement('h5'); h.textContent = '✦ ' + item.title;
        const paragraph = document.createElement('p'); paragraph.textContent = item.text; copy.append(h, paragraph); card.append(link, copy); showcase.append(card);
      }
      body.append(showcase);
    }
    if (project.demo) {
      const note = document.createElement('p'); note.className = 'pending';
      note.textContent = '此為版型示範，非謝沛璇本人實際作品。'; body.append(note);
    }
    const back = document.createElement('button'); back.className = 'project-back'; back.textContent = '← 回到上一頁・精選作品';
    back.addEventListener('click', () => {
      history.pushState(null, '', location.pathname + location.search + '#work');
      hideWindow(document.getElementById('detail'), true); openWindow('work');
    });
    body.append(back);
    document.getElementById('detail-title').textContent = project.title;
    openWindow('detail'); body.scrollTop = 0;
  }
  projects.forEach(project => {
    const card = document.createElement('a'); card.className = 'project gallery-card';
    card.href = project.link; card.setAttribute('aria-label', `${project.title} — ${project.category}，查看作品`);
    const image = new Image(); image.className = 'project-cover'; image.src = project.cover;
    image.alt = project.title + '作品封面'; image.width = 720; image.height = 600; image.decoding = 'async';
    const caption = document.createElement('span'); caption.className = 'project-caption';
    const title = document.createElement('strong'); title.textContent = project.title;
    const category = document.createElement('span'); category.textContent = project.category + ' ↗';
    caption.append(title, category); card.append(image, caption); grid.append(card);
    card.addEventListener('pointerenter', e => { if (e.pointerType === 'mouse') showPreview(card, project); });
    card.addEventListener('pointerleave', hidePreview);
    card.addEventListener('focus', () => showPreview(card, project));
    card.addEventListener('blur', hidePreview);
    card.addEventListener('click', event => {
      hidePreview();
      if (project.link.startsWith('#project/') && !event.ctrlKey && !event.metaKey && !event.shiftKey && !event.altKey) {
        event.preventDefault(); history.pushState(null, '', project.link); openProject(project);
      }
    });
  });
  function route() {
    const project = projects.find(p => p.link === location.hash);
    if (project) openProject(project);
    else if (!document.getElementById('detail').hidden) hideWindow(document.getElementById('detail'), true);
  }
  addEventListener('popstate', route); addEventListener('hashchange', route);
  addEventListener('resize', hidePreview); addEventListener('scroll', hidePreview, true);
  addEventListener('blur', hidePreview); fine.addEventListener('change', hidePreview);
  document.addEventListener('pointerdown', hidePreview, true);
  document.addEventListener('keydown', e => { if (e.key === 'Escape') hidePreview(); });
  // Every new visit starts at the welcome window, including shared project URLs.
  if (location.hash) history.replaceState(null, '', location.pathname + location.search);
  openWindow('welcome', false);
})();
